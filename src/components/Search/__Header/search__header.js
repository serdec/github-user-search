import React from 'react';
import styles from '../search.module.css';

const Search__Header = () => (
  <div className={styles.search__header}>
    <h2>Search Users on Github</h2>
  </div>
);

export default Search__Header;
